extends layout

block content
  h1 #{title}: #{item.name}
  p= item.description
    
    p Do you really want to delete this club?

    form(method='POST' action='')
      div.form-group
        input#itemid.form-control(type='hidden',name='itemid', required='true', value=item._id )

      button.btn.btn-primary(type='submit') Delete